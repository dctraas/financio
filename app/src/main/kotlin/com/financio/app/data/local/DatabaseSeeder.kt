package com.financio.app.data.local

import androidx.room.withTransaction
import com.financio.app.DefaultAccount
import com.financio.core.categorize.DefaultCategorization
import com.financio.core.model.MatchType
import javax.inject.Inject
import javax.inject.Singleton

/**
 * Seeds one-time defaults into an empty database:
 * - the single fase-1 account ([DefaultAccount]) that every transaction's `accountId` foreign
 *   key points at — without this, the very first import's insert fails with a
 *   `FOREIGN KEY constraint failed`, since `accounts` never gets a row otherwise. `AccountDao`
 *   already had an `insert()` from the original skeleton; nothing ever called it.
 * - the default categories and keyword rules, so a first import has something sensible to
 *   auto-categorize against instead of showing every transaction as "te controleren" against an
 *   empty category list — see the README's categorization UX notes for the full reasoning.
 *
 * Both are guarded by an existence/emptiness check rather than a uniqueness constraint: this is
 * a single local database with no concurrent writers, so "no account/categories yet" is a
 * reliable signal this has never run, and simpler than adding a migration for a one-time seed.
 * Both run in one transaction so a process death mid-seed can't leave things half-seeded — a
 * retried [seedIfEmpty] would otherwise see a non-empty table and skip the rest.
 */
@Singleton
class DatabaseSeeder @Inject constructor(
    private val database: FinancioDatabase,
    private val accountDao: AccountDao,
    private val categoryDao: CategoryDao,
    private val categoryRuleDao: CategoryRuleDao,
) {
    suspend fun seedIfEmpty() {
        database.withTransaction {
            seedDefaultAccount()
            seedDefaultCategories()
        }
    }

    private suspend fun seedDefaultAccount() {
        if (accountDao.get(DefaultAccount.ID) != null) return
        // Explicit id: DefaultAccount.ID is the constant every accountId FK and query in the app
        // assumes, not just "whatever the first autogenerated row happens to get".
        accountDao.insert(AccountEntity(id = DefaultAccount.ID, name = DefaultAccount.DISPLAY_NAME, ibanMasked = ""))
    }

    private suspend fun seedDefaultCategories() {
        if (categoryDao.count() > 0) return

        val categoryIds = DefaultCategorization.CATEGORIES.associate { category ->
            val id = categoryDao.insert(CategoryEntity(name = category.name, colorHex = category.colorHex))
            category.name to id
        }

        val rules = DefaultCategorization.KEYWORD_RULES.map { rule ->
            CategoryRuleEntity(
                categoryId = categoryIds.getValue(rule.categoryName),
                matchType = MatchType.KEYWORD.name,
                pattern = rule.keyword,
                priority = DefaultCategorization.PRIORITY,
            )
        }
        categoryRuleDao.insertAll(rules)
    }
}
