<?xml version="1.0" encoding="utf-8"?>
<!--
    Deliberately no <uses-permission> at all: fase 1 has no backend and no network path
    for data to leave the device (see the architecture doc's security section). Do not add
    android.permission.INTERNET here without first revisiting that decision.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Financio"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Financio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
